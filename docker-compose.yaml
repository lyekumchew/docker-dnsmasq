version: "3.1"

services:
  dnsmasq:
    image: lyekumchew/dnsmasq
    restart: always
    ports:
      - "5380:8080"
      - "53:53"
    volumes:
      - ./dnsmasq.conf:/etc/dnsmasq.conf
    environment:
      - HTTP_USER=foo
      - HTTP_PASS=ba